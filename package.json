{
  "name": "domain_checker",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "node-fetch": "^3.3.1",
    "nodemon": "^2.0.21"
  },
  "scripts": {
    "start": "npm run react-build && npm run server",
    "react-start": "react-scripts start",
    "react-build": "react-scripts build",
    "server-dev": "nodemon server/server.js",
    "server": "node server/server.js",
    "dev": "npm run react-build && npm run server-dev"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
